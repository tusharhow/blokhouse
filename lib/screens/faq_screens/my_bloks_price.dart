import 'package:flutter/material.dart';


class MyBloksPrice extends StatelessWidget {
  const MyBloksPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
      return Scaffold(
        body: Center(
          child: Text('My Bloks Price'),
        ),
      
      
      );
  }
}