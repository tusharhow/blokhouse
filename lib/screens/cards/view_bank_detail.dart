import 'package:flutter/material.dart';

class ViewBankdetail extends StatelessWidget {
  const ViewBankdetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
